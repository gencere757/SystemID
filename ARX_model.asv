z = iddata(y,u,1);

nk = dead_time;               % Your identified dead time (e.g., 2)
nb = length(input_lags_3_to_80); % Total number of input terms (e.g., 78)
na = max(top_5_output_lags);   % Max past output dependence (e.g., 5)

% Define the structural matrix
orders = [na, nb, nk];

